using System;

namespace circle
{
    class Program
    {
        static void Main(string[] args)
        {
            double radius = 3.9;
            double area = Math.PI * radius * radius;
            Console.WriteLine(area);
        }
    }
}
